# Prompt-based harness (GEPA / ACE / OpenEvolve) + shared cl/ eval library.
# Use a dedicated venv:  uv venv .venv-harness && uv pip install -e . (from repo root)
# (mirrors [project.dependencies] in pyproject.toml)
openevolve
gepa[full]
datasets
pyyaml
dspy>=3.1.3
langdetect
nltk
immutabledict
absl-py
wandb
python-dotenv
pyarrow>=23.0.1
litellm>=1.51
boto3>=1.43.2
portkey-ai>=2.3.0
sentence-transformers>=5.5.0
faiss-cpu>=1.13.2
